<script>
  // The one Author surface for a COMMITTED story — the full editor (document · plot · relationships ·
  // map). Folded in from the retired Overview page (see [[overview-is-editor]]). New-story genesis lives
  // separately at /stories/genesis (StructureWorkspace's cast-queue).
  import StoryWorkspace from '$lib/components/story/StoryWorkspace.svelte';
</script>

<StoryWorkspace />
