<script>
  // The narrative CHARACTER CARDS surface — base + tell/shape/full ladder, gated per arc STAGE.
  // Distinct from Outfits (/cast = the wardrobe catalogue). See [[storymaster-pacing]] (the gate is
  // the card content) + [[bond-depth-weave]].
  import CharacterCards from '$lib/components/story/CharacterCards.svelte';
</script>

<CharacterCards />
