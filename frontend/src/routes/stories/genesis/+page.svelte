<script>
  // From-scratch create entry → the one Structure workspace (no story key yet = the generate flow).
  // On commit it navigates to /stories/[key]/structure — the same component, now live.
  import StructureWorkspace from '$lib/components/story/StructureWorkspace.svelte';
</script>

<StructureWorkspace />
